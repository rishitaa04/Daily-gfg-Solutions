class Solution {
    int binarysearch(int arr[], int n, int k) {
        // code 
        for(int i = 0 ; i < n ;  i++){
            if(arr[i] == k ){
                return i;
            }
        }
        return -1;
    }
}
