class Solution{
    
    public boolean IsPerfect(int a[], int n)
    {
        // Complete the function
        int j=n-1;
        for(int i=0;i<n/2;i++)
        {
            if(a[i]!=a[j])
            return false;
            
            j--;
        }
        return true;
    }
    
}
